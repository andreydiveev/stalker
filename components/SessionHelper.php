<?php

class SessionHelper extends CComponent{
    /**
    * Иницилизация сессии
    */
    public static function initSession()
    {
        //$domain = '';// load from database
        //$path   = '';// load from database

        /** @var CHttpSession $session */
        //$session = Yii::app()->session;
        //$session->cookieParams = array(
        //'path'     => $path,
        //'domain'   => $domain,
        //'httpOnly' => true,
        //);
        //$session->open();
    }
}